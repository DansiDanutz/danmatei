# Cloud-API voice agent — LiveKit Agents framework with Inference proxy.
# No local models, no GPU, no Ollama sidecar.

# Core agent framework + plugins
livekit-agents[silero,turn-detector]>=1.2.0,<2.0.0

# Outbound webhook to /api/voice/webhook
httpx>=0.27.0

# Env loading
python-dotenv>=1.0.0

# Pydantic 2 is pulled transitively by livekit-agents; pin to avoid surprises
pydantic>=2.0.0
